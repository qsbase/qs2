#ifndef _QS2_QX_FILE_HEADERS_H_
#define _QS2_QX_FILE_HEADERS_H_

// qs2 forwards to the vendored qdata-cpp file-header implementation.
// That vendored header currently carries helpers for both qdata and qs2 so
// the shared logic stays in one place and cross-format messages stay useful.

#include "qdata_format/detail/file_headers.h"

#endif
